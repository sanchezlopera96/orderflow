# Normalize line endings: store as LF in the repo, check out native on each OS
* text=auto

# Force LF where it matters
*.sh text eol=lf

# Treat known binaries as binary (never diffed or normalized)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.zip  binary
